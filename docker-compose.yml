services:
  wickedyoda-helper:
    build: .
    container_name: wickedyoda-little-helper
    env_file:
      - env.env
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    restart: unless-stopped
